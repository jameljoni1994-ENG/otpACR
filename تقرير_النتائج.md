# تقرير نتائج: تحسين النموذج التكعيبي

عشر مراحل تجريبية. الرسوم: `experiments/figures/` — النتائج: `experiments/results/`.

---

## المنهجية

Cubic Regularization وامتداداته على مسائل تحليلية وMNIST، مع مقاييس f / ‖∇f‖ / الزمن / الدقة.

---

## ملخص 1–8

- **ARC+Krylov** قوي لتحسين f والتدرج؛ **L-BFGS** غالبًا الأسرع.
- على Softmax (dim=7840): L-BFGS≈ARC في f، لكن Krylov بـ M ثابت أعطى دقة اختبار أعلى قليلًا مع f أسوأ.

---

## المرحلة 9 — Early-stopping على validation

MNIST softmax، تقسيم train/val/test (~6400/1600/2000)، dim=7840.

| الطريقة | دقة اختبار | f تدريب | زمن |
|---|---:|---:|---:|
| **ARC-Krylov + ES** | **0.904** | 0.210 | 4.16ث |
| **L-BFGS + ES** | **0.902** | 0.230 | **0.29ث** |
| ARC كامل (بدون ES) | 0.869 | 0.042 | 4.72ث |
| L-BFGS كامل | 0.868 | 0.043 | 1.27ث |

الرسم: `24_early_stopping.png`

**استنتاج:** إيقاف مبكر حسب دقة التحقق رفع دقة الاختبار ~+3.5 نقطة لكلا الطريقتين، مع f تدريب أعلى (أقل overfit). **L-BFGS+ES** الأسرع عمليًا؛ **ARC+ES** ينافس بالدقة.

---

## المرحلة 10 — توحيد المسارات + توسع

تحسينات هندسية وتجارب توسع (`experiments/run_phase10.py`):

1. **ARC–Krylov الموحّد:** `arc.minimize(mode="krylov")` يستخدم `solve_inexact_step` و`theta_schedule` مع تشخيصات `history_resid` / `history_m` / `history_theta`.
2. **QN matrix-free:** حل المسألة الفرعية عبر `LBFGSHessian.matvec` دون بناء B كثيفة (`matrix_free=True` افتراضيًا).
3. **Stochastic+ARC:** ratio-test وتكييف M، مع تقييم كامل كل `eval_every` فقط.
4. **مسار l2 + ES:** `training.l2_path_early_stop` عبر شبكة λ مع warm-start.
5. **توسع:** MNIST موسّع (افتراضي ~12k؛ `--full` ≈60k) + قياس sketch–Krylov عند n∈{2k,8k,20k}.

### نتائج MNIST كامل (~60k، train/val/test ≈38400/9600/12000)

| الطريقة | دقة اختبار | زمن |
|---|---:|---:|
| **ARC-Krylov + ES** | **0.913** | 52.9ث |
| **L-BFGS + ES** | 0.910 | **3.7ث** |
| QN-CR matrix-free | 0.910 | 2.6ث |

للتوسع المتوسط (~12k): ARC-ES ≈0.921، L-BFGS-ES ≈0.914، QN-MF ≈0.897.

الرسوم: `25_phase10_mnist.png`, `26_phase10_l2_path.png`, `27_phase10_sketch_scale.png` — JSON: `experiments/results/phase10.json`.

```bash
py -3 experiments/run_phase10.py --quick          # دخان سريع
py -3 experiments/run_phase10.py                  # توسع متوسط
py -3 experiments/run_phase10.py --full           # MNIST كامل ~60k
```

---

## الخلاصة النهائية المحدّثة

1. لتحسين الحل الأمثلي (f / ‖∇f‖): **ARC-Krylov** (المسار الموحّد غير الدقيق).
2. للتعلم الآلي العام (دقة اختبار + سرعة): **L-BFGS + early-stopping** أو ARC+ES؛ جرّب مسار l2 عند الحاجة.
3. لا تُحسِّن حتى النهاية على بيانات محدودة — راقب validation.
4. θ التكيّفي وKrylov يبقيان ضروريين؛ QN التكعيبي أصبح قابلًا للأبعاد الكبيرة بدون Hessian كثيف (**مبرهنة mf-qn** في الأوراق).
5. Stochastic CR أقرب لسلوك ARC عبر اختبار النسبة وتكييف M.

---

## المرحلة 11 — دعم نظري وتجريبي لـ QN بلا كثافة

- تطابق `matvec` مع `to_dense @ v` حتى ~1e-15.
- اتفاق dense vs matrix-free على تربيعية: فجوة f/x ~1e-15.
- زمن `to_dense` / `matvec` عند n=800 ≈ **815×**.
- QN-CR بلا كثافة حتى n=4000 عملي (~0.28ث).

الرسوم: `28_phase11_qn_scale.png`, `29_phase11_bfgs_ops.png` — JSON: `experiments/results/phase11.json`.

```bash
py -3 experiments/run_phase11.py
```

---

## أوامر إعادة الإنتاج

```bash
pip install -e .
pytest -q
py -3 experiments/run_phase8.py
py -3 experiments/run_phase9.py
py -3 experiments/run_phase10.py --quick
py -3 experiments/run_phase11.py
```
